Server:
	Environment:
		python3 (http://www.runoob.com/python/python-intro.html)
			-flask (http://flask.pocoo.org/docs/1.0/)
	API:
		
		1.Get the document
		HTTP GET  */get?key=(Key)   
			
			return json{"sentence":(sentence)}
			
		2.Set the document
		HTTP POST */store   {"doc":(document)}
			
			return json{"key":(key)
			
		3.Get the document from database
		get_doc_from_database(key):
			return document
			
		4.Store the document to the datebase
		store_doc_to_database(doc):
			return key
			
		5.Create key:
		create_key():
			return String:key
			
		6.Convert chinese key to hash:
		chinese_key_to_hash(chinese_key):
			return hashkey[:lenth]
			
		
		To do later 
			1.User system